# Pharos default backend

Simple service to host the default backend for Pharos.

Fullfills the default-backend requirements:
> 1. It serves a 404 page at /
> 2. It serves 200 on a /healthz endpoint

